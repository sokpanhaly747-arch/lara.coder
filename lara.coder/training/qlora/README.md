# training/qlora/

QLoRA-specific training entrypoints and configs (4-bit quantized fine-tuning). See training/configs/sft_config.yaml for a working QLoRA config today; this folder is for QLoRA-only variants as they diverge from plain SFT.
