# debugging/recovery/

Future capability: rolling back to the last known-good state when fix attempts are exhausted (agent.config.LoopLimits.max_total_fix_attempts), rather than just failing. Not implemented yet.
