# app/components/

Shared frontend UI components for the builder's own interface (chat bubbles, file tree rows) — distinct from design/components, which is about the *generated product's* components.
