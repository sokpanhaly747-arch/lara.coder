# app/web/

Alias-clarifying folder: the Next.js UI described in ../frontend/README.md. Kept separate from ../frontend so 'web' (required by the architecture spec) and the existing frontend/ folder both exist without duplicating content.
