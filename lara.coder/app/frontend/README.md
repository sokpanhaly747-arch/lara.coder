# app/frontend/

Placeholder for the Next.js user interface (chat, file explorer, code
editor, live preview iframe, agent activity log). Scaffolding this is
layer-13 in `ARCHITECTURE.md` / `README.md`'s build order — it depends
on `app/backend` having a real WebSocket contract first.
