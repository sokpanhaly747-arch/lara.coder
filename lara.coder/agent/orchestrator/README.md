# agent/orchestrator/

Config/docs folder alongside ../orchestrator.py (the real phase-transition policy). Reserved for orchestrator policy variants (e.g. a stricter or more permissive LoopLimits profile) as data rather than code changes.
