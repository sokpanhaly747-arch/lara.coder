# agent/core/

Config/docs for the core loop machinery in ../loop.py, ../state.py, ../orchestrator.py, ../config.py. Those files remain the real implementation; this folder is reserved for splitting the loop into multiple files if it outgrows a single module.
