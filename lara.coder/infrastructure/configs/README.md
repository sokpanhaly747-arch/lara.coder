# infrastructure/configs/

Environment-specific settings overlays (dev/staging/prod). Kept separate from ../config/ (singular), which holds the base settings.yaml, to avoid touching that existing working file.
